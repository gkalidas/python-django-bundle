# while with list and indexes

ex_list = ['one', 'two', 'three', 'four']
counter = 0
max_index = len(ex_list) - 1

while counter <= max_index:
    number = ex_list[counter]
    print(number)
    counter += 1
